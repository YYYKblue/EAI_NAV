# 全局代价地图只保留与局部代价地图不同的参数。
global_costmap:
  global_frame: /map                # 全局代价地图固定在 map 坐标系。
  update_frequency: 1.5             # 地图内部更新频率，单位 Hz。
  publish_frequency: 1.0            # 完整代价地图发布频率，单位 Hz。
  rolling_window: false             # false：地图范围固定，与 map_server 地图一致。
